# 📝 AI 활용 프로젝트를 위한 초보자 깃허브(GitHub)

이 매뉴얼은 제미나이(Gemini)나 ChatGPT 등 AI 채팅을 통해 생성된 **마크다운(.md)** 문서와 **웹페이지(.html)** 파일을 깃허브에 안전하게 저장하고 관리(형상관리)하고 싶은 초보자를 위해 작성되었습니다.

---

## 1. 프로그램 설치 및 초기 설정

### 1) Git(깃) 설치하기
Git은 내 컴퓨터에서 파일의 변화를 기록하는 '엔진'입니다.
* **윈도우(Windows):** [git-scm.com](https://git-scm.com/) 접속 -> 'Download for Windows' 클릭 -> 모든 설정을 기본값(Next)으로 설치 완료.
* **맥(Mac):** 터미널 앱 실행 후 `git --version` 입력 -> 설치 안내창이 뜨면 '설치' 클릭.

### 2) 사용자 정보 등록 (최초 1회)
누가 기록을 남겼는지 확인하기 위해 이름과 이메일을 등록합니다.
* **윈도우:** 시작 메뉴에서 **'Git Bash'** 실행
* **맥:** **'터미널'** 실행
* 아래 명령어를 한 줄씩 입력 후 엔터:
    ```bash
    git config --global user.name "본인영문이름"
    git config --global user.email "깃허브가입이메일"
    ```

---

## 2. 깃허브 저장소(Repository) 만들기

1.  [GitHub.com](https://github.com/) 로그인.
2.  오른쪽 상단 **[+]** 아이콘 클릭 -> **[New repository]** 선택.
3.  **Repository name**에 프로젝트 이름 입력 (예: `my-ai-docs`).
4.  화면 맨 아래 **[Create repository]** 클릭.
5.  생성된 화면 중앙의 주소(`https://github.com/아이디/이름.git`)를 복사해둡니다.

---

## 3. 내 컴퓨터와 깃허브 연결하기 (최초 1회)

1.  컴퓨터에 작업용 **새 폴더**를 만듭니다 (예: `C:\my-project`).
2.  폴더 안에서 마우스 우클릭 -> **[Git Bash Here]** 클릭 (맥은 폴더 우클릭 -> 서비스 -> 터미널 열기).
3.  까만 창에 아래 명령어를 순서대로 입력합니다:
    ```bash
    git init
    git branch -M main
    git remote add origin [아까 복사한 깃허브 주소 붙여넣기]
    ```

---

## 4. 실전! 파일 저장 및 형상관리 (반복 루틴)

채팅으로 얻은 코드를 파일로 만들고 깃허브에 올릴 때마다 아래 순서를 따릅니다.

### 1단계: 파일 만들기
* 메모장 등을 열어 AI가 준 내용을 붙여넣습니다.
* **[다른 이름으로 저장]**을 선택해 확장자를 직접 입력합니다.
    * 문서: `파일명.md`
    * 웹화면: `파일명.html`

### 2단계: 깃허브에 업로드 (마법의 3줄 명령어)
파일을 저장한 폴더의 **Git Bash(터미널)** 창에서 다음 명령어를 입력합니다.

1.  **장바구니 담기 (Add)**
    ```bash
    git add .
    ```
    (모든 변경사항을 선택합니다.)

2.  **이름표 붙이기 (Commit)**
    ```bash
    git commit -m "무슨 파일인지 메모 작성"
    ```
    (예: `git commit -m "제미나이로 작성한 기획안 추가"`)

3.  **전송하기 (Push)**
    ```bash
    git push origin main
    ```

---

## 5. 형상관리(기록) 확인하기

* **진행 상황 확인:** `git status` (현재 어떤 파일이 수정되었는지 확인)
* **과거 기록 보기:** `git log --oneline` (그동안 남긴 메모 목록 확인)
* **웹에서 보기:** 깃허브 사이트 내 저장소의 **'commits'** 항목을 클릭하면, 날짜별로 변화된 내용을 시각적으로 비교해 볼 수 있습니다.

---

## 💡 초보자를 위한 꿀팁
* **자주 커밋하세요:** 파일이 완성될 때마다 기록(Commit)을 남기면 나중에 실수해도 과거로 되돌리기 쉽습니다.
* **확장자 주의:** 반드시 파일명 뒤에 `.md`나 `.html`이 정확히 붙었는지 확인하세요. (`test.md.txt`가 되지 않도록 주의!)
